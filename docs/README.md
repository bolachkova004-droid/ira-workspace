# Ira Workspace 7.5.1

Исправление ошибки Safari/Telegram WebView: `Attempted to assign to readonly property`.

Изменения:
- редактирование ученика теперь заменяет запись целиком, не меняя объект напрямую;
- абонемент сохраняется тем же безопасным способом;
- редактирование урока и автосписание абонемента также переведены на безопасное обновление;
- локальное сохранение не изменяет объект состояния напрямую.

Загрузите все файлы в папку `docs` репозитория с заменой.
