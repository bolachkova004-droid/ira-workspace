import {Download,Moon,RefreshCcw,Sun,Upload} from 'lucide-react';import PageHeader from '../components/PageHeader'
export default function Settings({dark,setDark,exportData,importData,reset}:{dark:boolean;setDark:(x:boolean)=>void;exportData:()=>string;importData:(x:string)=>void;reset:()=>void}){const download=()=>{const a=document.createElement('a');a.href=URL.createObjectURL(new Blob([exportData()],{type:'application/json'}));a.download='ira-workspace-backup.json';a.click()};const upload=(e:React.ChangeEvent<HTMLInputElement>)=>{const f=e.target.files?.[0];if(!f)return;const r=new FileReader();r.onload=()=>{try{importData(String(r.result));alert('Данные импортированы')}catch{alert('Не удалось прочитать файл')}};r.readAsText(f)};return <section className="screen"><PageHeader title="Настройки" subtitle="Внешний вид, резервные копии и данные"/><div className="settings-list"><section className="panel setting-row"><div>{dark?<Moon/>:<Sun/>}<span><strong>Тема приложения</strong><small>{dark?'Тёмная':'Светлая'}</small></span></div><button className="toggle" aria-label="Сменить тему" onClick={()=>setDark(!dark)}><i/></button></section><section className="panel"><span className="kicker">Резервная копия</span><h2>Твои данные остаются у тебя</h2><p className="muted">Скачай копию перед крупными изменениями или перенеси данные на другое устройство.</p><div className="button-row"><button className="secondary" onClick={download}><Download size={18}/>Экспорт</button><label className="secondary file"><Upload size={18}/>Импорт<input type="file" accept="application/json" onChange={upload}/></label></div></section><section className="panel danger-zone"><span className="kicker">Опасная зона</span><h2>Сбросить демо-данные</h2><p className="muted">Удалит локальные изменения и вернёт исходное содержимое.</p><button className="danger-btn" onClick={()=>confirm('Сбросить все данные?')&&reset()}><RefreshCcw size={18}/>Сбросить</button></section></div></section>}
